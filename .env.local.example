# Copy this file to .env.local and fill with your Firebase Web App config
# Never commit .env.local (it's ignored by git by default when added to .gitignore)
# These values come from Firebase Console → Project settings → Your apps → Web app → Config

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=

# Set to your Firebase Project ID. Based on your console screenshot, this is prefilled to "medsta".
VITE_FIREBASE_PROJECT_ID=medsta

VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
